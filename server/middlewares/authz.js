// const authz = async (req, res, next) => {
//   try {
//     // const { role } = req.user;
//     // const findMyCourse = await MyCourse.findByPk(id);
//     console.log(role);

//     if (role !== 'admin') throw { name: 'Forbidden' };

//     next();
//   } catch (err) {
//     next(err);
//   }
// };

// module.exports = authz;
